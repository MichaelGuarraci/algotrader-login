# ES bar feed. Run `python es_scan.py --probe` to find one that answers.
# Polygon's stocks plan does NOT cover futures.
FMP_API_KEY=
POLYGON_API_KEY=

# Override the built-in sources with a feed you actually have entitlement to
# (Polygon Futures, Databento, Tradovate, Rithmic, IBKR).
# ES_BARS_URL=

# Front-month contract. Update at roll: Mar / Jun / Sep / Dec.
ES_SYMBOL=ESUSD
